# Gated Design

Push back on compatibility work; gate so processing takes assumptions as granted, works straight, fails hard as designed.

Gate styles:
- Empty — clarify the assumptions, then go; no version needed.
- Versioned — source may differ; carry a version/type and route to different impls.
