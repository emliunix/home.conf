# visflow-programme - trigger cases, as canonical / trap / paraphrase triplets
# Derived from the batch task set (tasks-v5), which mapped 1:1 onto the ten surfaces.
skill: visflow-programme
dimension: trigger
triplets:
  - behavior: the work this skill owns
    canonical:
      prompt: "Open a new tree for the question: what happens when one node is enabled twice in the same step?"
      expected: [visflow-programme]
      cites: "SKILL.md:3-6 - the Use clause names this shape"
    trap:
      prompt: "Add it as one more round inside the taskboard-v3 tree."
      expected: []
      cites: "SKILL.md:3-6 - the exclusion names this shape, in this skill's own vocabulary"
    paraphrase:
      prompt: "I have a hunch about retry storms - where should this live?"
      expected: []
      cites: "SKILL.md:3-6 - the same shape with the vocabulary removed"
run_notes:
  - 'Arms: entry-only over the full catalog; the degenerate control is replaced by the distractor set, the excluded shapes, and route repetition.'
  - 'Scored items: T-SELECT, T-SILENT, T-TRAP, T-PARA. Ambiguity is a finding, never a miss.'
  - 'The trap and the paraphrase are the same situation; if only the trap stays silent, the surface is reading words, not the shape.'
