# 02 — L1 triplets for remaining unique tuples

Status: draft-followup

Seeded from `design/01-fact-based-eval.md` grill. No obligation until picked up.
When picked up, write the three heads and grill as a normal design.

## Functional unit

L1 triplets (canonical already in 01 `full`) for:

- `review-pass`
- `impl-pass`
- `retro-land`
- `round-budget-halt`
- `impl-defect`
- `reject-finding`

Each triplet: trap (fixture invites the `catches` route using skill vocabulary) + paraphrase (no skill terminology). Same `expected` tuple as the 01 canonical. One exported `DecisionCase[]` remains the only corpus; this wave only adds rows.
