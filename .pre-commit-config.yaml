repos:
  - repo: local
    hooks:
      - id: document-contracts
        name: document contracts
        language: system
        entry: npm exec doc-verify -- check --staged --profile auto --format text
        pass_filenames: false
        files: '(^|/).+\.(md|ya?ml)$|^doc-verify/|^package(-lock)?\.json$'
        require_serial: true
